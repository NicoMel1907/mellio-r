% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{mt_compare}
\alias{mt_compare}
\title{Compare multiple models side-by-side}
\usage{
mt_compare(
  ...,
  style = "apa7",
  title = NULL,
  number = NULL,
  note = NULL,
  source = NULL,
  column.labels = NULL,
  dep.var.labels = NULL,
  se.type = c("parentheses", "inline", "none"),
  stars = TRUE,
  stats = c("n", "r.squared", "adj.r.squared", "f.statistic"),
  decimals = 2L,
  p_decimals = 3L,
  omit = NULL,
  intercept.bottom = TRUE
)
}
\arguments{
\item{...}{Model objects to compare}

\item{style}{Citation style (default "apa7")}

\item{title}{Table title}

\item{number}{Table number}

\item{note}{Table note (NULL auto-generates significance note when stars = TRUE)}

\item{source}{Table source text}

\item{column.labels}{Character vector of model labels (e.g., c("Step 1", "Step 2")).
Defaults to "Model 1", "Model 2", etc.}

\item{dep.var.labels}{Dependent variable label (rendered as spanner across
all model columns)}

\item{se.type}{How to display standard errors: "parentheses" (default,
separate indented row), "inline" (same cell), "none" (omit)}

\item{stars}{Logical, append significance stars (default TRUE)}

\item{stats}{Character vector of model statistics for bottom rows.
Default: c("n", "r.squared", "adj.r.squared", "f.statistic").
Available: "n", "r.squared", "adj.r.squared", "f.statistic",
"aic", "bic", "loglik", "residual.se"}

\item{decimals}{Decimal places for coefficients (default 2)}

\item{p_decimals}{Decimal places for p-values (default 3)}

\item{omit}{Character vector of term names to exclude}

\item{intercept.bottom}{Place intercept at bottom of coefficient rows
(default TRUE)}
}
\value{
A melliotab object
}
\description{
Creates a stargazer-style comparison table with models as columns,
stacked coefficient/SE rows, significance stars, and model statistics.
}
\details{
Accepts any model objects supported by \code{broom::tidy()} and
\code{broom::glance()}, including lm, glm, and lme4 (via broom.mixed).
You can call this helper directly, or pass multiple model objects to
\code{\link[=melliotab]{melliotab()}} or \code{\link[=mellio_open]{mellio_open()}}, which delegate here.
}
\examples{
m1 <- lm(Ozone ~ Temp, data = airquality)
m2 <- lm(Ozone ~ Temp + Wind, data = airquality)
m3 <- lm(Ozone ~ Temp + Wind + Solar.R, data = airquality)
mt_compare(
  m1, m2, m3,
  title = "Predictors of ozone concentration",
  column.labels = c("Step 1", "Step 2", "Step 3"),
  dep.var.labels = "Ozone concentration"
)
}
