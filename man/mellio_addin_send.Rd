% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge-addin.R
\name{mellio_addin_send}
\alias{mellio_addin_send}
\title{RStudio addin — Send the selected R expression to Mellio}
\usage{
mellio_addin_send()
}
\value{
Invisibly \code{NULL}. Side effect: opens the browser at Mellio.
}
\description{
Reads the active selection in the RStudio editor, evaluates it in
the global environment, and opens the result as a Result Card in
the Mellio web app. The selected text is preserved as the displayed
call so the saved card still says e.g. "t.test(score ~ group, data
= df)" rather than the evaluated variable.
}
\details{
Registered via \code{inst/rstudio/addins.dcf}. Appears in RStudio's
\strong{Addins} menu once the package is loaded.
}
\examples{
\donttest{
if (interactive() &&
    requireNamespace("rstudioapi", quietly = TRUE) &&
    rstudioapi::isAvailable()) {
  # Highlight   t.test(extra ~ group, data = sleep)   in RStudio
  # Addins menu -> Send to Mellio
  mellio_addin_send()
}
}
}
\seealso{
Other R bridge: 
\code{\link{mellio_capture}()},
\code{\link{mellio_compare}()},
\code{\link{mellio_open}()},
\code{\link{mellio_payload}()},
\code{\link{mellio_to_json}()},
\code{\link{print.mellio_payload}()}
}
\concept{R bridge}
