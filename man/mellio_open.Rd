% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mellio-open.R
\name{mellio_open}
\alias{mellio_open}
\title{Open a table, statistical result, or plot in Mellio}
\usage{
mellio_open(x, ..., browse = TRUE, .call = NULL)
}
\arguments{
\item{x}{An R object supported by Mellio.}

\item{...}{Additional arguments forwarded to the relevant Mellio
constructor or payload method.}

\item{browse}{Open the browser? \code{TRUE} by default. Set \code{FALSE} to
return the URL without launching anything.}

\item{.call}{Internal captured user call for provenance.}
}
\value{
Invisibly, the URL string.
}
\description{
Unified entry point for sending R objects to the Mellio web app.
Statistical results and tables open as Stats Result Cards; plots open
in the web app's Figures editor.
}
\section{Supported inputs}{

\code{mellio_open()} routes objects by class:
\itemize{
\item Statistical results such as \code{htest}, \code{lm}, \code{glm}, \code{aov}, \code{lavaan},
mixed-model, survival, ordinal, mediation, and selected optional-package
result classes open as structured Result Cards.
\item Data frames, matrices, base \code{table} objects, and non-statistical
\code{melliotab} objects open in the Tables workspace.
\item \code{ggplot2}, lattice, htmlwidget, recorded plot, and supported image-file
inputs open in Figures.
}

Optional model and figure integrations use packages listed in \code{Suggests}.
If an optional package is not installed, the corresponding object class is
skipped or reported with an informative message.
}

\section{Privacy and destination}{

\code{mellio_open()} opens \verb{https://www.mellioapp.com} by default. Advanced
users can set \code{options(mellio.editor_url = "https://...")}, but should only
use a trusted Mellio deployment. The R payload is encoded in the URL
fragment. URL fragments are not sent as HTTP requests to the server, but the
full URL can still be visible to the browser, the opened web app, browser
history, extensions, and anyone the URL is shared with.

By default, Mellio payloads include R/package-version metadata and data
fingerprints where available. Local machine details are opt-in:
\code{options(mellio.provenance = "full")}. To omit provenance metadata, use
\code{options(mellio.provenance = FALSE)}.
}

\section{Plot size limits}{

Plots are transported through the URL hash. Very large images may exceed
browser URL limits; reduce \code{width}, \code{height}, or \code{dpi}, or save the image and
upload it manually in Mellio.
}

\examples{
\donttest{
if (interactive()) {
  mellio_open(t.test(extra ~ group, data = sleep))

  m1 <- lm(Ozone ~ Temp, data = airquality)
  m2 <- lm(Ozone ~ Temp + Wind, data = airquality)
  m3 <- lm(Ozone ~ Temp + Wind + Solar.R, data = airquality)
  mellio_open(
    m1, m2, m3,
    labels = c("Step 1", "Step 2", "Step 3"),
    dep.var.labels = "Ozone concentration"
  )
}
}
}
\seealso{
\code{\link[=mt_compare]{mt_compare()}} for model comparison table options.

Other R bridge: 
\code{\link{mellio_addin_send}()},
\code{\link{mellio_capture}()},
\code{\link{mellio_compare}()},
\code{\link{mellio_payload}()},
\code{\link{mellio_to_json}()},
\code{\link{print.mellio_payload}()}
}
\concept{R bridge}
