% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge-extract-comparison.R
\name{mellio_compare}
\alias{mellio_compare}
\title{Compare nested regression models as a Mellio Stats card}
\usage{
mellio_compare(..., labels = NULL, .call = NULL)
}
\arguments{
\item{...}{Nested model objects, usually \code{lm} objects ordered from the
baseline model to the final model.}

\item{labels}{Optional model labels. Defaults to \code{"Model 1"}, \code{"Model 2"},
and so on.}

\item{.call}{Optional call string for provenance / display.}
}
\value{
A \code{mellio_payload} object.
}
\description{
Builds a hierarchical-regression comparison payload from two or more
nested model objects. Unlike \code{mellio_payload(anova(m1, m2))}, this keeps the
original models available so Mellio can report model \code{R^2}, adjusted \code{R^2},
\verb{Delta R^2}, and the nested-model F-change.
}
\examples{
m1 <- lm(mpg ~ wt + hp, data = mtcars)
m2 <- lm(mpg ~ wt + hp + cyl, data = mtcars)
p <- mellio_compare(m1, m2)
p$type
}
\seealso{
Other R bridge: 
\code{\link{mellio_addin_send}()},
\code{\link{mellio_capture}()},
\code{\link{mellio_open}()},
\code{\link{mellio_payload}()},
\code{\link{mellio_to_json}()},
\code{\link{print.mellio_payload}()}
}
\concept{R bridge}
