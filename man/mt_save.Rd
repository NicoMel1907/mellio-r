% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{mt_save}
\alias{mt_save}
\title{Save a melliotab table to file}
\usage{
mt_save(x, filename, ...)
}
\arguments{
\item{x}{A melliotab object}

\item{filename}{Output file path. Supported extensions: .html, .tex, .md}

\item{...}{Additional arguments passed to format-specific functions}
}
\value{
Invisible file path
}
\description{
Auto-detects the format from the file extension.
}
\examples{
model <- lm(mpg ~ wt + hp, data = mtcars)
tbl <- melliotab(model, style = "apa7", title = "Results")

html_file <- tempfile(fileext = ".html")
tex_file <- tempfile(fileext = ".tex")
md_file <- tempfile(fileext = ".md")

mt_save(tbl, html_file)
mt_save(tbl, tex_file)
mt_save(tbl, md_file)
}
